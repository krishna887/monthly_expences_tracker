spring.application.name=monthly_expense_tracker
spring.security.user.name=k
spring.security.user.password=k
server.port=${PORT:8080}
# Database Configuration
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}

#spring.datasource.url=jdbc:mysql://localhost:3306/monthly_expense_tracker?useSSL=false&allowPublicKeyRetrieval=true
#spring.datasource.username=root
#spring.datasource.password=Rrka@123

# Hibernate & JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

